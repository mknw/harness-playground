services:
  gateway:
    image: docker/mcp-gateway
    command:
      - --servers=context7,fetch,neo4j-cypher,neo4j-memory
      - --transport=streaming
      - --port=3000
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      - "3000:3000"
